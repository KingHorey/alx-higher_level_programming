#!/usr/bin/pyhton3

""" NO modules imported"""


def magic_string():
    """This function returns a string n times the number
    of iteration"""
    magic_srings = getattr(magic_string, "n", 0) + 1
    return ', '.join(["BestSchool" for i in range(magic_strings)])
